name: expiration-ci

on:
  push:
    branches: [ main ]
    paths:
      - "expiration/**"
  pull_request:
    branches: [ main ]
    paths:
      - "expiration/**"

jobs:
  build-and-push-expiration:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.CR_PAT }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push expiration image
        uses: docker/build-push-action@v5
        with:
          context: expiration
          file: expiration/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/expiration:${{ github.sha }}
